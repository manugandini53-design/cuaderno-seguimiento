release-v*/

# Claves/certificados y config de entorno — defensa en profundidad por si
# alguna vez se crea o copia un archivo así en este repo (el empaquetado
# Android/desktop, y sus keystores/claves de firma, ahora viven en los repos
# cuaderno-android y cuaderno-desktop).
*.keystore
*.jks
*.p12
*.pem
*.key
keystore.properties
.env
.env.*
